# HF Transformers Route

Use this route when the source clearly belongs to Hugging Face transformers.

Top-level `model-migrate` should hand off to the existing
`hf-transformers-migrate` capability instead of duplicating its detailed
workflow.
