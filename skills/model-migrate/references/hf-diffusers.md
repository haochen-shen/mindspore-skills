# HF Diffusers Route

Use this route when the source clearly belongs to Hugging Face diffusers.

Top-level `model-migrate` should hand off to the existing
`hf-diffusers-migrate` capability instead of duplicating its detailed workflow.
