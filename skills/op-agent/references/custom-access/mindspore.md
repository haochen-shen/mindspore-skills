# MindSpore Custom-Access

Use this reference when the selected method is `custom-access` and the target
framework is `mindspore`.

Focus on:

- custom-op or plugin workspace layout
- registration and Python access surface
- build entry and smoke-test path
