# Operator Spec

Before implementing an operator, capture:

- framework
- backend
- operator name
- inputs and outputs
- attrs
- dtype and shape requirements
- forward-only or backward-required
- expected delivery artifact

Do not start coding until the operator spec is minimally stable.
